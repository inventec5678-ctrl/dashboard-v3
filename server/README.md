# Server

Dashboard V3 的後端直接使用 Dashboard V2 的 server.py。

**請勿修改本目錄下的任何檔案 — 後端保持穩定，不跟著前端重構。**

如需修改 server.py，請到：
`../dashboard_v2_standalone/server.py`

啟動方式：
```bash
cd ../dashboard_v2_standalone
python server.py
# Server 运行在 http://localhost:5006
```
